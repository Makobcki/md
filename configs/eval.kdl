config target="eval" version=2 {
  model {
    use "mmdit_576"
    checkpoint null
  }

  sampler {
    use "rf_heun"
  }

  eval {
    prompt_root "data/eval_prompts"
    out_dir "runs/eval"
    seed 42
    steps 28
    cfg 4.5
    n_per_prompt 1
  }
}
