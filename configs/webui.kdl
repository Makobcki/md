config target="webui" version=2 {
  model {
    use "mmdit_576"
    checkpoint "checkpoints/model.safetensors"
  }

  sampler {
    use "rf_euler"
  }

  webui {
    use "local"
    host "127.0.0.1"
    port 7860
    auto_open true
  }
}
