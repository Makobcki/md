config target="train" version=1 {
  model {
    use "../presets/model/mmdit_1024.kdl"
    checkpoint null
  }

  vae {
    pretrained "stabilityai/sd-vae-ft-mse"
    scaling_factor 0.18215
    downsample_factor 8
    latent_channels 4
  }

  text {
    use "../presets/text/clip_l_t5_base.kdl"
  }

  training {
    use "../presets/training/bf16_adamw.kdl"
    batch_size 1
    grad_accum_steps 8
    max_steps 100000
  }

  data {
    use "../presets/data/latent_cache_1024.kdl"
    dataset_path "data/dataset"
  }

  output {
    dir "runs/train_1024"
  }
}
