config target="train" version=2 {
  model {
    use "mmdit_576"
    checkpoint null
  }

  training {
    use "bf16_adamw"
    batch_size 4
    grad_accum_steps 1
    max_steps 100000
  }

  data {
    use "latent_cache_576"
    dataset_path "data/train"
  }

  output {
    dir "runs/train"
  }
}
