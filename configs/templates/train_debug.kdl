config target="train" version=2 {
  model {
    use "mmdit_576"
    checkpoint null
  }

  training {
    use "single_gpu_bf16_debug"
  }

  data {
    use "latent_cache_576"
    dataset_path "data/train"
  }

  output {
    dir "runs/debug"
  }
}
